import { AppConfirmDirective } from './app-confirm.directive';

describe('AppConfirmDirective', () => {
  it('should create an instance', () => {
    const directive = new AppConfirmDirective();
    expect(directive).toBeTruthy();
  });
});
